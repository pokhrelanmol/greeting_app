const Add = (a, b) => {
	let sum = a + b;
	return sum;
};
const subtract = (a, b) => {
	let sub = a - b;
	return a - b;
};
const multiply = (a, b) => {
	let mul = a * b;
	return mul;
};
const divide = (a, b) => {
	let div = a / b;
	return div;
};

export { Add, subtract, multiply, divide };
